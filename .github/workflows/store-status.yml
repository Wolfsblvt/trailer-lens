name: Store status

# Manual dispatch, protected environment: fetch and record the truthful
# Chrome Web Store item state around the owner's publication decision.
#
# The final public transition of an approved STAGED_PUBLISH submission is
# an explicit owner action in the Chrome Web Store Developer Dashboard —
# deliberately not automated for this product. This lane verifies state
# before and after that decision; it performs no upload, submission, or
# publication, and the rollout-percentage endpoint is deliberately never
# used (it only adjusts an already-published revision and is gated to
# items with more than 10,000 seven-day active users).

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version whose Store state is being verified (e.g. 1.1.0)'
        required: true

permissions:
  contents: read

concurrency:
  group: chrome-web-store
  cancel-in-progress: false

jobs:
  status:
    runs-on: ubuntu-latest
    environment: chrome-web-store
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Acquire a Chrome Web Store access token
        id: token
        env:
          CWS_WIF_PROVIDER: ${{ vars.CWS_WIF_PROVIDER }}
          CWS_SERVICE_ACCOUNT: ${{ vars.CWS_SERVICE_ACCOUNT }}
          CWS_CLIENT_ID: ${{ secrets.CWS_CLIENT_ID }}
          CWS_CLIENT_SECRET: ${{ secrets.CWS_CLIENT_SECRET }}
          CWS_REFRESH_TOKEN: ${{ secrets.CWS_REFRESH_TOKEN }}
        run: |
          set -euo pipefail
          if [ -n "$CWS_WIF_PROVIDER" ] && [ -n "$CWS_SERVICE_ACCOUNT" ]; then
            OIDC=$(curl -sSf -H "Authorization: Bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
              "$ACTIONS_ID_TOKEN_REQUEST_URL&audience=https://iam.googleapis.com/$CWS_WIF_PROVIDER" | jq -r .value)
            STS=$(curl -sSf https://sts.googleapis.com/v1/token \
              -d grant_type=urn:ietf:params:oauth:grant-type:token-exchange \
              -d audience="//iam.googleapis.com/$CWS_WIF_PROVIDER" \
              -d scope=https://www.googleapis.com/auth/cloud-platform \
              -d requested_token_type=urn:ietf:params:oauth:token-type:access_token \
              -d subject_token_type=urn:ietf:params:oauth:token-type:jwt \
              -d subject_token="$OIDC" | jq -r .access_token)
            TOKEN=$(curl -sSf -H "Authorization: Bearer $STS" -H "Content-Type: application/json" \
              "https://iamcredentials.googleapis.com/v1/projects/-/serviceAccounts/$CWS_SERVICE_ACCOUNT:generateAccessToken" \
              -d '{"scope":["https://www.googleapis.com/auth/chromewebstore"]}' | jq -r .accessToken)
          elif [ -n "$CWS_CLIENT_ID" ] && [ -n "$CWS_REFRESH_TOKEN" ]; then
            TOKEN=$(curl -sSf https://oauth2.googleapis.com/token \
              -d client_id="$CWS_CLIENT_ID" -d client_secret="$CWS_CLIENT_SECRET" \
              -d refresh_token="$CWS_REFRESH_TOKEN" -d grant_type=refresh_token | jq -r .access_token)
          else
            echo "No Store credentials configured; see docs/RELEASES.md." >&2
            exit 1
          fi
          echo "::add-mask::$TOKEN"
          echo "token=$TOKEN" >> "$GITHUB_OUTPUT"

      - name: Fetch and record the item state
        env:
          TOKEN: ${{ steps.token.outputs.token }}
        run: |
          curl -sSf -H "Authorization: Bearer $TOKEN" \
            "https://chromewebstore.googleapis.com/v2/publishers/${{ vars.CWS_PUBLISHER_ID }}/items/${{ vars.CWS_ITEM_ID }}:fetchStatus" \
            | tee status.json
          echo
          echo "Publication of an approved staged submission for ${{ inputs.version }} is"
          echo "performed by the owner in the Chrome Web Store Developer Dashboard."
          echo "This lane only records the truthful state; API state and public"
          echo "CDN-visible availability can lag each other."
